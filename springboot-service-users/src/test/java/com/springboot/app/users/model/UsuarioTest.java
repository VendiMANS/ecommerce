package com.springboot.app.users.model;

import static org.junit.jupiter.api.Assertions.*;

import java.util.ArrayList;
import java.util.HashSet;
import java.util.List;
import java.util.Set;

import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;

class UsuarioTest {
/*
	List<String> permisosVacio;
	List<String> permisosNoVacio;
	
	Usuario vacio;
	Usuario noVacio;
	
	@BeforeEach
	public void setup() {
		
		vacio = new Usuario();
		noVacio = new Usuario("usuario", "pass", "permiso");
		noVacio.setId(1L);
	}
	
	
	
	@Test
	void testGetId() {
		assertNull(vacio.getId());
	}

	@Test
	void testSetId() {
		assertEquals(1L, noVacio.getId());
		
		noVacio.setId(2L);
		
		assertEquals(2L, noVacio.getId());
	}

	@Test
	void testGetUsername() {
		assertNull(vacio.getUsername());
	}

	@Test
	void testSetUsername() {
		assertEquals("usuario", noVacio.getUsername());
		
		noVacio.setUsername("admin");
		
		assertEquals("admin", noVacio.getUsername());
	}

	@Test
	void testGetPassword() {
		assertNull(vacio.getPassword());
	}

	@Test
	void testSetPassword() {
		assertEquals("pass", noVacio.getPassword());
		
		noVacio.setPassword("pasos");
		
		assertEquals("pasos", noVacio.getPassword());
	}

	@Test
	void testGetPermiso() {
		assertNull(vacio.getPermiso());
	}

	@Test
	void testSetPermiso() {
		assertEquals("permiso", noVacio.getPermiso());
		
		noVacio.setPermiso("permiso nuevo");
		
		assertEquals("permiso nuevo", noVacio.getPermiso());
	}
*/
}
